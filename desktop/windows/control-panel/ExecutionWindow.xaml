<Window x:Class="AgentDock.ControlPanel.ExecutionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AgentDock.ControlPanel"
        Title="AgentDock" Width="1280" Height="800" MinWidth="800" MinHeight="540"
        FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="14"
        Background="{DynamicResource AppBackground}" Foreground="{DynamicResource PrimaryText}"
        Loaded="Window_Loaded" Closed="Window_Closed" SizeChanged="Window_SizeChanged" PreviewKeyDown="Window_KeyDown">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Themes/Tokens.xaml"/>
        <ResourceDictionary Source="Themes/Light.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      <BooleanToVisibilityConverter x:Key="BoolVisibility"/>
      <Style x:Key="RowMore" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Opacity" Value="0"/><Setter Property="Width" Value="28"/><Setter Property="Padding" Value="0"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Style.Triggers>
          <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True"><Setter Property="Opacity" Value="1"/></DataTrigger>
          <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True"><Setter Property="Opacity" Value="1"/></DataTrigger>
          <DataTrigger Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True"><Setter Property="Opacity" Value="1"/></DataTrigger>
        </Style.Triggers>
      </Style>
      <DataTemplate x:Key="CallRowTemplate">
        <Grid Height="{StaticResource ExecutionRowHeight}" Margin="6,0">
          <Grid.ColumnDefinitions><ColumnDefinition Width="28"/><ColumnDefinition Width="*"/><ColumnDefinition Width="84"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <TextBlock Text="{Binding StatusGlyph}" ToolTip="{Binding State}" AutomationProperties.Name="{Binding State}" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
              <Style.Triggers><DataTrigger Binding="{Binding Status}" Value="failed"><Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/></DataTrigger><DataTrigger Binding="{Binding NeedsApproval}" Value="True"><Setter Property="Foreground" Value="{DynamicResource DangerBrush}"/></DataTrigger></Style.Triggers>
            </Style></TextBlock.Style>
          </TextBlock>
          <TextBlock Grid.Column="1" Text="{Binding Title}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Title}" VerticalAlignment="Center" Margin="7,0,14,0"/>
          <TextBlock Grid.Column="2" Text="{Binding Duration}" Style="{StaticResource MutedText}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,0"/>
          <TextBlock Grid.Column="3" Width="78" Text="{Binding When}" Style="{StaticResource MutedText}" VerticalAlignment="Center" HorizontalAlignment="Right" TextAlignment="Right" Visibility="{Binding ShowTimestamps, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolVisibility}}"/>
        </Grid>
      </DataTemplate>
    </ResourceDictionary>
  </Window.Resources>
  <Grid>
    <Grid.ColumnDefinitions><ColumnDefinition Width="240" MinWidth="200" MaxWidth="320"/><ColumnDefinition Width="1"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
    <Border Background="{DynamicResource SidebarBackground}" Padding="12,12,10,12">
      <DockPanel>
        <Grid DockPanel.Dock="Top" Margin="4,0,0,12">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="30"/></Grid.ColumnDefinitions>
          <Grid>
            <TextBox x:Name="SearchBox" TextChanged="Search_Changed" ToolTip="{local:Loc Execution_SearchWorkspaceOrConversation}" AutomationProperties.Name="{local:Loc Execution_SearchWorkspaceOrConversation}" Background="Transparent"/>
            <TextBlock Text="{local:Loc Execution_Search}" Foreground="{DynamicResource SecondaryText}" Margin="9,0" VerticalAlignment="Center" IsHitTestVisible="False">
              <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
            </TextBlock>
          </Grid>
          <Button Grid.Column="1" Content="···" Padding="0" Click="SidebarMenu_Click" ToolTip="{local:Loc Execution_ConversationManagement}" AutomationProperties.Name="{local:Loc Execution_ConversationManagement}"/>
        </Grid>
        <Button x:Name="MoreObjectsButton" DockPanel.Dock="Bottom" Content="{local:Loc Execution_MoreConversations}" Click="MoreObjects_Click" Visibility="Collapsed"/>
        <Grid>
          <ListBox x:Name="ObjectsList" ItemsSource="{Binding Objects}" SelectionMode="Extended" SelectionChanged="Objects_Changed" PreviewMouseRightButtonDown="Objects_RightClick"
                   VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.IsVirtualizingWhenGrouping="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
            <ListBox.GroupStyle>
              <GroupStyle>
                <GroupStyle.ContainerStyle>
                  <Style TargetType="GroupItem"><Setter Property="Template"><Setter.Value><ControlTemplate TargetType="GroupItem">
                    <Expander IsExpanded="True" Loaded="WorkspaceGroup_Loaded" Expanded="WorkspaceGroup_Expanded" Collapsed="WorkspaceGroup_Collapsed" Margin="0,0,0,8">
                      <Expander.Template><ControlTemplate TargetType="Expander">
                        <StackPanel>
                          <ToggleButton IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" HorizontalContentAlignment="Stretch" Background="Transparent" BorderThickness="0" Height="34">
                            <ToggleButton.Template><ControlTemplate TargetType="ToggleButton">
                              <Border Background="Transparent" Padding="3,0,6,0"><Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="18"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock x:Name="Arrow" Text="▸" Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding Name.Title}" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding ItemCount}" Foreground="{DynamicResource SecondaryText}" FontSize="12" Margin="8,0,0,0" VerticalAlignment="Center"/>
                              </Grid></Border>
                              <ControlTemplate.Triggers><Trigger Property="IsChecked" Value="True"><Setter TargetName="Arrow" Property="Text" Value="▾"/></Trigger></ControlTemplate.Triggers>
                            </ControlTemplate></ToggleButton.Template>
                          </ToggleButton>
                          <ContentPresenter x:Name="GroupItems" ContentSource="Content" Margin="16,0,0,0"/>
                        </StackPanel>
                        <ControlTemplate.Triggers><Trigger Property="IsExpanded" Value="False"><Setter TargetName="GroupItems" Property="Visibility" Value="Collapsed"/></Trigger></ControlTemplate.Triggers>
                      </ControlTemplate></Expander.Template>
                      <ItemsPresenter/>
                    </Expander>
                  </ControlTemplate></Setter.Value></Setter></Style>
                </GroupStyle.ContainerStyle>
              </GroupStyle>
            </ListBox.GroupStyle>
            <ListBox.ItemTemplate><DataTemplate>
              <Grid Height="34" Margin="8,0,3,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="28"/></Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Title}" Foreground="{DynamicResource PrimaryText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding Title}"/>
                <TextBlock Grid.Column="1" Text="{Binding PendingCount}" FontSize="12" Margin="6,0,2,0" VerticalAlignment="Center" Foreground="{DynamicResource DangerBrush}">
                  <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Visible"/><Style.Triggers><DataTrigger Binding="{Binding PendingCount}" Value="0"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                </TextBlock>
                <Button Grid.Column="2" Content="···" Style="{StaticResource RowMore}" Click="ObjectMore_Click" ToolTip="{local:Loc Execution_ManageThisConversation}" AutomationProperties.Name="{local:Loc Execution_ManageThisConversation}"/>
              </Grid>
            </DataTemplate></ListBox.ItemTemplate>
          </ListBox>
          <TextBlock x:Name="SidebarEmpty" Text="{local:Loc Execution_NoConversations}" Foreground="{DynamicResource SecondaryText}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
        </Grid>
      </DockPanel>
    </Border>
    <Border Grid.Column="1" Background="{DynamicResource SeparatorBrush}"/>
    <Grid Grid.Column="2" Background="{DynamicResource PanelBackground}">
      <Grid.RowDefinitions><RowDefinition Height="56"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="44"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
      <Grid x:Name="ConversationHeader" Margin="20,0,16,0">
        <Grid.ColumnDefinitions><ColumnDefinition Width="*" MinWidth="60"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <TextBlock x:Name="ObjectTitle" Text="{local:Loc Execution_SelectConversation}" FontSize="20" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Margin="0,0,12,0"/>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <Button x:Name="AttentionButton" Content="{local:Loc Execution_Attention}" Visibility="Collapsed" Click="Attention_Click" FontSize="12"/>
          <Button Content="{local:Loc Execution_Permissions}" Click="Permissions_Click" FontSize="12"/>
          <Button x:Name="ConnectionButton" Content="{local:Loc Execution_Connection}" Click="Connection_Click" Foreground="{DynamicResource SecondaryText}" FontSize="12"/>
          <Button Content="{local:Loc Execution_Theme}" Click="Theme_Click" FontSize="12"/>
          <Button Content="{local:Loc Execution_Settings}" Click="SettingsMenu_Click" FontSize="12"/>
          <Button x:Name="StopConversationButton" Content="{local:Loc Execution_Stop}" Click="Terminate_Click" Style="{StaticResource DangerButton}" Visibility="Collapsed" Margin="8,0,0,0" FontSize="12"/>
          <Button Content="···" Click="ConversationMenu_Click" Width="30" Padding="0" Margin="2,0,0,0" ToolTip="{local:Loc Execution_CurrentConversationActions}" AutomationProperties.Name="{local:Loc Execution_CurrentConversationActions}"/>
        </StackPanel>
      </Grid>
      <Border x:Name="WarningPanel" Grid.Row="1" Visibility="Collapsed" Background="{DynamicResource SelectionBackground}" Margin="20,0,20,8" Padding="10,5" CornerRadius="5">
        <DockPanel><Button DockPanel.Dock="Right" Content="{local:Loc Execution_Close}" FontSize="12" Click="DismissWarning_Click"/><TextBlock x:Name="WarningText" TextWrapping="Wrap" VerticalAlignment="Center"/></DockPanel>
      </Border>
      <Border x:Name="ConversationProgressCard" Grid.Row="2" Visibility="Collapsed" Margin="20,0,20,8">
        <Grid Height="36">
          <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="50"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <ComboBox x:Name="TaskChoiceCombo" DisplayMemberPath="Title" SelectedValuePath="Id" SelectionChanged="TaskChoice_Changed" ToolTip="{local:Loc Execution_RelatedTaskTooltip}" Margin="0,0,8,0"/>
          <TextBlock x:Name="CurrentTaskStatus" Grid.Column="1" Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12"/>
          <ProgressBar x:Name="CurrentTaskProgress" Grid.Column="2" Maximum="100" VerticalAlignment="Center" Margin="0,0,8,0"/>
          <TextBlock x:Name="CurrentTaskNext" Grid.Column="3" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center" Margin="0,0,6,0" FontSize="12"/>
          <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center"><Button x:Name="FilterTaskButton" Content="{local:Loc Execution_FilterThisTask}" Click="FilterTask_Click" FontSize="12"/><Button Content="{local:Loc Execution_TaskDetails}" Click="TaskDetails_Click" FontSize="12"/></StackPanel>
        </Grid>
      </Border>
      <Grid Grid.Row="3" Margin="20,0,20,8">
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <Grid Width="150"><TextBox x:Name="CallSearchBox" TextChanged="CallSearch_Changed" AutomationProperties.Name="{local:Loc Execution_SearchCalls}"/><TextBlock Text="{local:Loc Execution_SearchCalls}" Foreground="{DynamicResource SecondaryText}" Margin="9,0" VerticalAlignment="Center" IsHitTestVisible="False"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding Text, ElementName=CallSearchBox}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock></Grid>
          <ComboBox x:Name="CallStatusCombo" Width="92" Margin="8,0,0,0" SelectionChanged="CallFilter_Changed" SelectedIndex="0"><ComboBoxItem Content="{local:Loc Execution_StatusAll}" Tag=""/><ComboBoxItem Content="{local:Loc Execution_StatusRunning}" Tag="running"/><ComboBoxItem Content="{local:Loc Execution_StatusPendingApproval}" Tag="pending_approval"/><ComboBoxItem Content="{local:Loc Execution_StatusFailed}" Tag="failed"/><ComboBoxItem Content="{local:Loc Execution_StatusUnknown}" Tag="unknown"/></ComboBox>
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
          <Button x:Name="OlderCallsButton" Content="{local:Loc Execution_Older}" Click="OlderCalls_Click" IsEnabled="False" FontSize="12"/>
          <Button x:Name="FollowButton" Content="{local:Loc Execution_Follow}" Click="Follow_Click" FontSize="12" ToolTip="{local:Loc Execution_FollowLatestCalls}"/>
          <Button Content="···" Width="30" Padding="0" Click="CallMenu_Click" ToolTip="{local:Loc Execution_RecordManagement}" AutomationProperties.Name="{local:Loc Execution_RecordManagement}"/>
        </StackPanel>
      </Grid>
      <Grid Grid.Row="4" Margin="14,0,14,8">
        <ListBox x:Name="CallsList" ItemsSource="{Binding Calls}" ItemTemplate="{StaticResource CallRowTemplate}" SelectionMode="Extended"
                 SelectionChanged="Calls_Changed" PreviewMouseRightButtonDown="Calls_RightClick" PreviewMouseWheel="Calls_Wheel" ScrollViewer.ScrollChanged="Calls_ScrollChanged"
                 VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
          <ListBox.ItemContainerStyle><Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}"><Setter Property="Height" Value="{StaticResource ExecutionRowHeight}"/></Style></ListBox.ItemContainerStyle>
        </ListBox>
        <StackPanel x:Name="EmptyPanel" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"><TextBlock x:Name="EmptyText" Text="{local:Loc Execution_NoExecutionRecords}" Foreground="{DynamicResource SecondaryText}"/><Button Content="{local:Loc Execution_Refresh}" Click="Refresh_Click" Margin="0,8,0,0"/></StackPanel>
      </Grid>
      <Border x:Name="DetailsPanel" Grid.Row="6" Height="270" Visibility="Collapsed" Background="{DynamicResource PanelBackground}" BorderBrush="{DynamicResource SeparatorBrush}" BorderThickness="0,1,0,0" Padding="18,5,18,12">
        <Border.Effect><DropShadowEffect BlurRadius="6" ShadowDepth="0" Opacity="0.08"/></Border.Effect>
        <DockPanel>
          <DockPanel DockPanel.Dock="Top" Margin="0,0,0,4"><Button DockPanel.Dock="Right" Content="{local:Loc Execution_Close}" Click="CloseDetails_Click" FontSize="12"/><TextBlock x:Name="DetailsTitle" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/></DockPanel>
          <Grid>
            <TabControl x:Name="CallDetailsTabs" SelectionChanged="DetailTab_Changed" Visibility="Collapsed">
              <TabItem Header="{local:Loc Execution_TabCommandOutput}"><DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,3,0,5"><Button Content="{local:Loc Execution_CopyCommand}" Click="CopyCommand_Click" FontSize="12"/><Button Content="{local:Loc Execution_CopyOutput}" Click="CopyOutput_Click" FontSize="12"/><Button Content="{local:Loc Execution_StopCall}" Click="StopCall_Click" IsEnabled="{Binding CanStop}" FontSize="12"/><Button Content="{local:Loc Execution_Approve}" Click="Approve_Click" Visibility="{Binding NeedsApproval, Converter={StaticResource BoolVisibility}}" FontSize="12"/><Button Content="{local:Loc Execution_Reject}" Click="Reject_Click" Visibility="{Binding NeedsApproval, Converter={StaticResource BoolVisibility}}" FontSize="12"/><Button Content="{local:Loc Execution_RetryInstruction}" Click="RetryInstruction_Click" IsEnabled="{Binding CanRetry}" FontSize="12"/></StackPanel>
                <TextBox DockPanel.Dock="Top" Text="{Binding Command, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" MaxHeight="64" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" BorderThickness="0" Padding="4,0"/>
                <TextBox Text="{Binding Output, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" BorderThickness="0" Padding="4,6"/>
              </DockPanel></TabItem>
              <TabItem Header="{local:Loc Execution_TabSource}"><TextBox x:Name="SourceDetailsText" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" BorderThickness="0"/></TabItem>
              <TabItem Header="{local:Loc Execution_TabChildren}"><ListBox x:Name="ChildrenList" ItemsSource="{Binding Children}" ItemTemplate="{StaticResource CallRowTemplate}" SelectionChanged="ChildCall_Changed"/></TabItem>
              <TabItem Header="{local:Loc Execution_TabTechnical}"><TextBox Text="{Binding Technical, Mode=OneWay}" IsReadOnly="True" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" BorderThickness="0"/></TabItem>
            </TabControl>
            <DockPanel x:Name="TaskDetailsPanel" Visibility="Collapsed">
              <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6"><ComboBox x:Name="BranchCombo" Width="170" DisplayMemberPath="Title" SelectedValuePath="Id" SelectionChanged="Branch_Changed"/><Button Content="{local:Loc Execution_ContinueFromBranch}" Click="ContinueBranch_Click" FontSize="12"/><Button Content="{local:Loc Execution_CopyContinueInstruction}" Click="ContinueTask_Click" FontSize="12"/><Button Content="{local:Loc Execution_ManageTask}" Click="TaskMenu_Click" FontSize="12"/></StackPanel>
              <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel><TextBlock x:Name="TaskGoalText" TextWrapping="Wrap" Margin="0,0,0,8"/><TextBlock x:Name="TaskStepsText" TextWrapping="Wrap" Margin="0,0,0,8"/><TextBlock x:Name="TaskAcceptanceText" TextWrapping="Wrap" Margin="0,0,0,8"/><TextBlock x:Name="MilestonesText" TextWrapping="Wrap" Foreground="{DynamicResource SecondaryText}"/></StackPanel></ScrollViewer>
            </DockPanel>
            <TextBox x:Name="InfoDetailsText" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" BorderThickness="0" Visibility="Collapsed"/>
            <DockPanel x:Name="DataManagementPanel" Visibility="Collapsed">
              <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6"><ComboBox x:Name="DataKindCombo" Width="130" SelectionChanged="DataFilter_Changed" SelectedIndex="0"><ComboBoxItem Content="{local:Loc Execution_DataHistoricalTasks}" Tag="task"/><ComboBoxItem Content="{local:Loc Execution_DataUnattributedCalls}" Tag="unattributed"/><ComboBoxItem Content="{local:Loc Execution_DataAllCalls}" Tag="call"/></ComboBox><ComboBox x:Name="DataViewCombo" Width="100" Margin="8,0" SelectionChanged="DataFilter_Changed" SelectedIndex="0"><ComboBoxItem Content="{local:Loc Execution_DataCurrent}" Tag="active"/><ComboBoxItem Content="{local:Loc Execution_DataArchived}" Tag="archived"/><ComboBoxItem Content="{local:Loc Execution_DataIsolated}" Tag="isolated"/><ComboBoxItem Content="{local:Loc Execution_DataTrash}" Tag="trash"/></ComboBox><Button Content="{local:Loc Execution_ManageSelected}" Click="ManageData_Click" FontSize="12"/><Button Content="{local:Loc Execution_Export}" Click="ExportData_Click" FontSize="12"/><Button x:Name="MoreDataButton" Content="{local:Loc Execution_More}" Click="MoreData_Click" FontSize="12" Visibility="Collapsed"/></StackPanel>
              <ListBox x:Name="ManagedObjectsList" SelectionMode="Extended" PreviewMouseRightButtonDown="Data_RightClick"><ListBox.ItemTemplate><DataTemplate><Grid Height="34" Margin="8,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="110"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/><TextBlock Grid.Column="1" Text="{Binding Detail}" Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center" HorizontalAlignment="Right"/></Grid></DataTemplate></ListBox.ItemTemplate></ListBox>
            </DockPanel>
          </Grid>
        </DockPanel>
      </Border>
    </Grid>
  </Grid>
</Window>
